import { connect } from 'react-redux'
import MedicalStaffList from '../components/medicalStaff/medicalStaffList/MedicalStaffList'

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(MedicalStaffList)
